from src.data_extraction.arrange_data import arrange_data

# Data = arrange_data(DEPLOY=True).start()
#
# def test__COUNTRY_DATA():
#     for i in Data.COUNTRY:
#         assert "url" in i and len(i["url"]) > 0
#         assert "name" in i and len(i["name"]) > 0
#         assert "flag" in i and len(i["flag "]) > 0
#
#     pass